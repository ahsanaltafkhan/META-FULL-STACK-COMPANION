# Django Setup Practice

```bash
python -m venv .venv
source .venv/bin/activate
pip install django
django-admin startproject config .
python manage.py runserver
```

Then inspect the generated project and identify settings, URLs, and the WSGI/ASGI entry points.
